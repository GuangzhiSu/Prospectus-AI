# Knowledge Module

Knowledge-graph and document-structure code lives here.

- `prospectus_docgraph/` - prospectus document graph package
- `requirements-docgraph.txt` - optional dependencies for the document graph tooling

The Python import name remains `prospectus_docgraph`; add `knowledge-module/` to `PYTHONPATH` when running this package directly.
