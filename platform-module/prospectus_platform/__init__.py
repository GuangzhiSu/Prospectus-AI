"""
Platform module — orchestrates the AI module via contract v1 job manifests.

Everything except AI agents lives on the platform side: web UI, file uploads,
prompts, KG config, export, packaging hooks.
"""

__version__ = "0.1.0"
