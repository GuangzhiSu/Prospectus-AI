# Platform

Runtime shells, local services, and distribution helpers live here.

- `desktop/` - Electron shell for the packaged UI
- `services/local-llm/` - optional legacy local RAG/LLM FastAPI service

The main AI runtime is isolated separately in `ai-module/`.
